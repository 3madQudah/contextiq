--extra-index-url https://download.pytorch.org/whl/cpu

# --- Web framework / API ---
fastapi==0.135.2
uvicorn==0.42.0
pydantic==2.13.4
python-multipart==0.0.22

# --- Auth ---
python-jose==3.5.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 breaks on bcrypt>=4.1 (reads a removed __about__ attribute); pin below it.
bcrypt<4.1
email-validator==2.3.0
cryptography==46.0.3
# Rate limiting for /api/auth/register and /api/auth/login.
slowapi==0.1.10

# --- Database (app DB + user-connected DBs for the text-to-SQL feature) ---
sqlalchemy==2.0.43
psycopg2-binary==2.9.12
PyMySQL==1.1.2

# --- LLM / RAG stack ---
langchain==1.2.14
langchain-core==1.5.2
langchain-community==0.4.1
langchain-classic==1.0.3
langchain-text-splitters==1.1.1
langchain-groq==1.1.3
langchain-huggingface==1.2.2
rank_bm25==0.2.2
faiss-cpu==1.13.2
# Explicit (not left transitive via sentence-transformers): pinning it here is
# what makes the --extra-index-url above actually apply to torch specifically,
# rather than pip resolving it on its own and grabbing the default CUDA build.
torch==2.13.0
sentence-transformers==5.3.0

# --- Document loaders (pdf/docx/csv; .md and .txt use the stdlib + these) ---
pypdf==6.14.2
python-docx==1.2.0

# --- Data ---
pandas==2.3.3

# --- Env ---
python-dotenv==1.1.0

# --- Dev / test only (not required to run the app) ---
pytest==8.4.2
